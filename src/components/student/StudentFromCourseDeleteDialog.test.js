import React from 'react';
import { render, screen } from '../../test/render.js';
import StudentFromCourseDeleteDialog from './StudentFromCourseDeleteDialog';

it('renders', () => {
    render(<StudentFromCourseDeleteDialog />);
});
