import React from 'react';
import PropTypes from 'prop-types';
import './Student.css';

const Student = () => {
  return (
    <>
      <Outlet />
    </>
  );
};

Student.propTypes = {
};

export default Student;
